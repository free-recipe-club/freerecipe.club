---
phase: 05-community-contributions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - README.md
  - app/controllers/home/controller.tsx
autonomous: false
requirements: [COMM-03, COMM-06]

must_haves:
  truths:
    - "The repo has a welcoming, accurate README reflecting the current alpha Remix stack"
    - "A beginner contributor can follow CONTRIBUTING.md to fork, clone, edit, and submit a recipe PR"
    - "A non-technical person can find clear email/mail instructions for submitting a recipe"
    - "The repo has a CODE_OF_CONDUCT.md (Contributor Covenant)"
    - "The repo has Hacktoberfest topic, labels, and starter issues"
  artifacts:
    - path: "CONTRIBUTING.md"
      provides: "Full contributor walkthrough with recipe format reference and non-technical submission path"
      contains: "## How to Submit a Recipe"
    - path: "CODE_OF_CONDUCT.md"
      provides: "Contributor Covenant v2.1"
      contains: "Contributor Covenant"
    - path: "README.md"
      provides: "Rewritten README with current stack, project structure, and contribution links"
      contains: "Remix"
    - path: "app/controllers/home/controller.tsx"
      provides: "Home page with non-technical submission blurb"
      contains: "recipes@freerecipe.club"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "app/data/recipe-schema.ts"
      via: "Recipe format reference documents same fields as Zod schema"
      pattern: "title.*byline.*location.*components.*directions"
    - from: "CONTRIBUTING.md"
      to: ".github/PULL_REQUEST_TEMPLATE/"
      via: "References PR template for recipe submissions"
      pattern: "pull request|PR template"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "Links to contributor guide"
      pattern: "CONTRIBUTING"
    - from: "app/controllers/home/controller.tsx"
      to: "CONTRIBUTING.md"
      via: "Home page blurb directs non-technical visitors toward email path documented in CONTRIBUTING.md"
      pattern: "recipes@freerecipe.club"
---

<objective>
Create contributor documentation (CONTRIBUTING.md, CODE_OF_CONDUCT.md, README rewrite), add non-technical submission blurb to home page, and set up Hacktoberfest readiness.

Purpose: Make the repo welcoming for first-time contributors (both technical and non-technical) and ready for Hacktoberfest participation.
Output: 3 documentation files + home page update + Hacktoberfest GitHub setup verified
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-community-contributions/05-CONTEXT.md
@.planning/phases/05-community-contributions/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/controllers/home/controller.tsx (current state):
```typescript
import { render } from '../render.tsx'

export function home() {
  return render(
    'Home',
    `<main class="max-w-2xl mx-auto px-4 py-16 text-center">
      <h1 class="text-4xl font-bold mb-4" style="color:var(--theme-accent)">freerecipe.club</h1>
      <p class="text-xl mb-8" style="color:var(--theme-text-secondary)">Recipes without the ads.</p>
      <p style="color:var(--theme-text-secondary)">No tracking. No accounts. No dark patterns. Just recipes.</p>
    </main>`
  )
}
```

From app/data/recipe-schema.ts (fields for CONTRIBUTING.md reference):
```typescript
RecipeSchema fields: title, byline, location, components (string[][]), directions (string | string[])[], background, links ({text, url}[]), flavor, pack (optional)
```

From package.json (scripts for README):
```json
"dev": "npm run dev:server & npm run dev:css",
"dev:server": "tsx watch server.ts",
"dev:css": "npx @tailwindcss/cli -i ./app/styles/input.css -o ./public/styles/output.css --watch",
"start": "tsx server.ts",
"build:css": "npx @tailwindcss/cli -i ./app/styles/input.css -o ./public/styles/output.css --minify",
"typecheck": "tsc --noEmit",
"validate": "tsx scripts/validate-recipes.ts"
```

Project structure (for README):
```
data/recipes/       — Recipe YAML files (e.g., pumpkin_doughnut.yml)
data/packs/         — Pack metadata YAML files
public/recipes/     — Recipe images (.jpg, matching recipe filename)
app/                — Application code (controllers, routes, data loading)
scripts/            — Validation scripts
.github/            — PR and issue templates, CI workflow
```

Stack: Alpha Remix 3 (remix-run/remix), Tailwind v4, TypeScript, Zod, Node.js, Fly.io
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CONTRIBUTING.md, CODE_OF_CONDUCT.md, and rewrite README.md</name>
  <files>
    CONTRIBUTING.md
    CODE_OF_CONDUCT.md
    README.md
  </files>
  <read_first>
    README.md
    app/data/recipe-schema.ts
    data/recipes/pumpkin_doughnut.yml
    package.json
    .planning/phases/05-community-contributions/05-CONTEXT.md
    .planning/PROJECT.md
  </read_first>
  <action>
Create 3 files at the repo root:

**1. `CONTRIBUTING.md`** — Per D-08, D-12, D-15, D-16. Beginner-friendly full walkthrough. Structure:

```markdown
# Contributing to freerecipe.club

Welcome! We'd love your recipes (and code contributions too).

## How to Submit a Recipe

### Via Pull Request (Recommended)

Full fork/clone/edit/commit/PR walkthrough:
1. Fork the repo on GitHub
2. Clone your fork: `git clone https://github.com/YOUR_USERNAME/freerecipe.club.git`
3. Create a branch: `git checkout -b add-my-recipe`
4. Add your recipe YAML file to `data/recipes/` (use underscores in filename, e.g., `chocolate_cake.yml`)
5. Add a matching `.jpg` image to `public/recipes/` (same filename, e.g., `chocolate_cake.jpg`)
6. Run `npm run validate` to check your recipe format
7. Commit and push your changes
8. Open a pull request — the recipe template will guide you

### Via GitHub Issue

Not comfortable with Git? Open a "Recipe Submission" issue and paste your recipe details. A maintainer will format it and create the PR for you.

### Via Email or Letter

Don't use GitHub at all? No problem!

**Email:** recipes@freerecipe.club
**Mail:** [Address TBD]

Include: recipe name, who made it, where you're from, ingredients (grouped by component), numbered steps, the story behind it, and source links if applicable. We'll create a GitHub issue, format your recipe, and credit you.

The process: Your recipe is received → a maintainer creates a GitHub issue → the recipe is formatted as YAML and added via PR → you're credited as the contributor.

## Recipe Format Reference

Recipes are YAML files. Here's the structure (all fields shown):

{Show a condensed example based on pumpkin_doughnut.yml with comments explaining each field}

### Required Fields
- `title` — Recipe name (max 100 characters)
- `byline` — Who made/submitted it
- `location` — Where the contributor is from
- `components` — Ingredient groups (each group: header + ingredients)
- `directions` — Step groups (each group: header + steps), or standalone steps

### Optional Fields
- `background` — The story behind the recipe
- `links` — Source/credit links (array of text + url)
- `flavor` — Short tagline
- `pack` — Pack slug if part of a themed pack (leave blank for independent recipes)

## Code Contributions

{Standard section: fork, branch, make changes, test, PR. Reference npm run typecheck and validate.}

## Running Locally

{npm install, npm run dev, visit localhost:3000}

## Questions?

Open an issue or email recipes@freerecipe.club
```

**2. `CODE_OF_CONDUCT.md`** — Per D-10. Use Contributor Covenant v2.1 verbatim. Set enforcement contact to `recipes@freerecipe.club` (per D-14, placeholder email). This is standard — copy the full text from https://www.contributor-covenant.org/version/2/1/code_of_conduct/ and only customize the contact method field.

**3. `README.md`** — Per D-09. Full rewrite replacing outdated Astro references. Structure:

```markdown
# freerecipe.club

Your always-ad-free source for recipes from the heart.

No tracking. No accounts. No dark patterns. Just recipes.

## What Is This?

A community-driven recipe website built as the antithesis of ad-bloated recipe sites. {2-3 sentences from PROJECT.md core description}

## Features

- Clean, recipe-first display — no preamble, no life stories before ingredients
- Cooking mode — step-by-step with wake lock for kitchen use
- Themed recipe packs — seasonal visual editions
- Print-friendly — clean printout without browser chrome
- Zero tracking, zero cookies, zero third-party scripts

## Contributing

We'd love your recipes! See [CONTRIBUTING.md](CONTRIBUTING.md) for full details.

**Quick paths:**
- 🍳 **Submit a recipe** — [Create a PR](CONTRIBUTING.md#via-pull-request-recommended) or [open an issue](CONTRIBUTING.md#via-github-issue)
- 📧 **Not a developer?** — Email recipes@freerecipe.club
- 🎃 **Hacktoberfest** — Look for issues labeled `good first issue` and `hacktoberfest`

## Running Locally

Requires Node.js 20+.

\`\`\`bash
npm install
npm run dev
\`\`\`

Visit `http://localhost:3000`

## Tech Stack

- [Alpha Remix 3](https://github.com/remix-run/remix) — Server framework
- [Tailwind CSS v4](https://tailwindcss.com/) — CSS-first styling
- [TypeScript](https://www.typescriptlang.org/) — Type safety
- [Zod](https://zod.dev/) — Recipe data validation
- Deployed on [Fly.io](https://fly.io/)

## Project Structure

\`\`\`
data/recipes/       Recipe YAML files
data/packs/         Themed pack metadata
public/recipes/     Recipe images
app/                Controllers, routes, data loading
scripts/            Validation scripts
.github/            Templates and CI
\`\`\`

## License

[MIT](LICENSE)

## Code of Conduct

[Contributor Covenant v2.1](CODE_OF_CONDUCT.md)
```

Do NOT include badges/shields (agent's discretion — keep it minimal per analog ethos).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const checks=[['CONTRIBUTING.md','How to Submit a Recipe'],['CONTRIBUTING.md','recipes@freerecipe.club'],['CONTRIBUTING.md','Via Email or Letter'],['CODE_OF_CONDUCT.md','Contributor Covenant'],['README.md','Remix'],['README.md','CONTRIBUTING.md'],['README.md','Hacktoberfest']]; let ok=true; for(const [f,s] of checks){if(!fs.existsSync(f)){console.error('MISSING: '+f);ok=false;continue;} if(!fs.readFileSync(f,'utf8').includes(s)){console.error(f+' missing: '+s);ok=false;}} if(ok) console.log('All docs verified'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` exists at repo root and contains `## How to Submit a Recipe`
    - `CONTRIBUTING.md` contains `recipes@freerecipe.club` (email path per D-14)
    - `CONTRIBUTING.md` contains `Via Email or Letter` section (per D-12, D-15)
    - `CONTRIBUTING.md` contains `npm run validate` (validation reference)
    - `CONTRIBUTING.md` contains recipe format reference with fields: title, byline, location, components, directions
    - `CODE_OF_CONDUCT.md` exists and contains `Contributor Covenant`
    - `CODE_OF_CONDUCT.md` contains `recipes@freerecipe.club` as enforcement contact
    - `README.md` contains `Remix` (not `Astro`) — confirming the rewrite
    - `README.md` contains link to `CONTRIBUTING.md`
    - `README.md` contains `Hacktoberfest` mention
    - `README.md` contains `npm run dev` (running locally section)
  </acceptance_criteria>
  <done>CONTRIBUTING.md has full fork-to-PR walkthrough, recipe format reference, and non-technical submission paths (issue, email, physical mail). CODE_OF_CONDUCT.md is Contributor Covenant v2.1. README.md is rewritten with current stack, features, contributing links, and Hacktoberfest section. No outdated Astro references remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add non-technical submission blurb to home page</name>
  <files>
    app/controllers/home/controller.tsx
  </files>
  <read_first>
    app/controllers/home/controller.tsx
    app/controllers/render.tsx
    .planning/phases/05-community-contributions/05-CONTEXT.md
  </read_first>
  <action>
Per D-13 and D-14, add a small blurb to the home page for non-technical visitors. This is intentionally tiny — just a sentence and placeholder email, not a full page.

Edit `app/controllers/home/controller.tsx`. Add a new paragraph after the existing content, inside the `<main>` tag. The blurb should be:

```html
<p class="mt-12 text-sm" style="color:var(--theme-text-secondary)">
  Have a recipe to share? Email us at
  <a href="mailto:recipes@freerecipe.club" class="underline" style="color:var(--theme-accent)">recipes@freerecipe.club</a>
  — no GitHub account needed.
</p>
```

This is placed below the existing "No tracking. No accounts. No dark patterns. Just recipes." line. Keep the existing content unchanged — only ADD the new paragraph.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const c=fs.readFileSync('app/controllers/home/controller.tsx','utf8'); const checks=['recipes@freerecipe.club','mailto:recipes@freerecipe.club','no GitHub account needed']; let ok=true; for(const s of checks){if(!c.includes(s)){console.error('Missing: '+s);ok=false;}} if(ok) console.log('Home page blurb verified'); process.exit(ok?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `app/controllers/home/controller.tsx` contains `recipes@freerecipe.club`
    - `app/controllers/home/controller.tsx` contains `mailto:recipes@freerecipe.club`
    - `app/controllers/home/controller.tsx` contains `no GitHub account needed`
    - The existing home page content (heading, tagline, no-tracking line) is preserved unchanged
    - The blurb uses theme CSS variables (`var(--theme-text-secondary)`, `var(--theme-accent)`)
  </acceptance_criteria>
  <done>Home page has a small, themed blurb directing non-technical visitors to email their recipes. Existing content unchanged. Uses theme CSS custom properties for consistent styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Hacktoberfest GitHub setup</name>
  <files>N/A — GitHub web UI actions</files>
  <action>
Complete Hacktoberfest GitHub setup (per D-11). These steps require GitHub web UI access:

1. Add repo topic: Go to repo Settings → click "Topics" gear icon → add `hacktoberfest`
2. Create labels (if they don't exist):
   - `hacktoberfest` (any color, e.g., #ff7518)
   - `good first issue` (should already exist as a GitHub default)
   - `recipe` (for recipe submissions)
   - `bug` (should already exist)
   - `tooling` (for AI skills template)
   - `enhancement` (should already exist)
3. Create 2-3 starter issues with `good first issue` + `hacktoberfest` labels:
   - "Add your favorite recipe" — invite contributors to submit a recipe that means something to them
   - "Add a holiday recipe" — seasonal recipe invitation
   - (Optional) "Improve recipe documentation" — for non-recipe contributions

Also verify that all docs created in Tasks 1-2 render correctly on GitHub.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const files=['CONTRIBUTING.md','CODE_OF_CONDUCT.md','README.md']; let ok=true; for(const f of files){if(!fs.existsSync(f)){console.error('MISSING: '+f);ok=false;}} if(ok) console.log('All contributor docs exist'); process.exit(ok?0:1)"</automated>
  </verify>
  <done>Hacktoberfest topic added to repo, labels created, starter issues posted, all contributor docs verified on GitHub.</done>
</task>

</tasks>

<verification>
1. `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md`, `README.md` exist at repo root with correct content
2. Home page shows non-technical submission blurb with email link
3. README no longer references Astro — fully updated to current alpha Remix stack
4. Hacktoberfest labels, topic, and starter issues configured on GitHub
</verification>

<success_criteria>
- A new contributor landing on the repo sees a welcoming README with clear paths to contribute
- CONTRIBUTING.md walks through the full recipe submission process for all skill levels
- Non-technical visitors see the email submission option on the home page
- The repo is Hacktoberfest-ready with proper topic, labels, and inviting starter issues
</success_criteria>

<output>
After completion, create `.planning/phases/05-community-contributions/05-02-SUMMARY.md`
</output>
